// import MarkdownIt from 'markdown-it';
import { describe, expect, it } from 'vitest';
// import { default as replPlugin } from '../src/index.js';

describe('itcatplayit/vitepress-markdown-it-repl > repl-plugin', () => {
  // const md = MarkdownIt().use(replPlugin);

  it('should extract', () => {
    expect('').toEqual('');
  });
});
